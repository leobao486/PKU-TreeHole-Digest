# 项目代码

源代码位于 `src/pku_treehole_digest`，测试位于 `tests`。公开副本使用通用示例配置；请在仓库根目录把 `个人画像.example.yaml` 复制为被 Git 忽略的 `个人画像.yaml` 后再运行。

程序只根据 HTML 报告文件名中的时间戳确定扫描起点。普通历史报告帖子不会被重新请求；收藏夹中的帖子会单独按洞号刷新，且不调用 DeepSeek。
